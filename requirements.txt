# Core RAG stack (same as the original repo)
chromadb>=0.5
sentence-transformers>=2.7
openai>=1.40
langchain-text-splitters>=0.2

# Guardrails additions
pypdf>=4.0                      # PDF ingestion
presidio-analyzer>=2.2          # optional PII enhancement (NLP)
presidio-anonymizer>=2.2

# UI backend
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# Dev / tests
pytest>=8.0